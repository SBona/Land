class Port
{
  private PVector loc;
  //angle for the approach, only can approach within a certain angle of this angle
  private PVector angle;
  
  public Port(PVector loc,PVector angle)
  {
    this.loc = loc;
    this.angle = angle;
  }
  
  
}